# Red-team run: live

Run 2026-09-06T03:10:52.786Z against https://rrmacademy.org.

**150 cases: 83 PASS, 0 FAIL, 0 KNOWN, 67 SKIP.**

| Family | Cases | PASS | FAIL | KNOWN | SKIP |
|---|---:|---:|---:|---:|---:|
| auth | 59 | 33 | 0 | 0 | 26 |
| money | 20 | 10 | 0 | 0 | 10 |
| pii | 24 | 16 | 0 | 0 | 8 |
| leak | 17 | 10 | 0 | 0 | 7 |
| headers | 16 | 14 | 0 | 0 | 2 |
| cost | 14 | 0 | 0 | 0 | 14 |

## Findings and adjudicated failures

None. Every case answered what the table says it must.
## Skipped, and why

- 1 cases: an expired session row can only be planted in the database this harness seeds
- 1 cases: needs a seeded blocked account with a live session
- 1 cases: requires a stored session id, which only a database read would give
- 10 cases: needs a real member session
- 1 cases: needs a real moderator session
- 1 cases: needs a real superadmin session
- 4 cases: needs two real member sessions
- 1 cases: a live run never revokes a real key
- 4 cases: needs a real non-member session
- 1 cases: needs a real unverified session
- 1 cases: a live run never spends a real login-rate-limit budget for a real address
- 2 cases: a live pair would consume the login limiter for a real address
- 1 cases: a live run never redeems a real reset token
- 1 cases: a live run never logs in as a real account
- 1 cases: a live run never attempts a signup against a real address
- 1 cases: a live run never changes a real password
- 4 cases: a live run never sends a webhook event to production
- 15 cases: spend assertions are only observable in process
- 2 cases: a live run never creates a real checkout session
- 1 cases: a live run never spends a real checkout-rate-limit budget
- 3 cases: a live run never submits a real survey
- 2 cases: a live run never posts a contact form
- 1 cases: a live run has no seeded token to probe
- 2 cases: a live run never posts to the real community
- 1 cases: a live run never breaks a production binding
- 1 cases: a live pair would send a real reset mail to a real address
- 1 cases: a live pair would create a real account
- 1 cases: the live run targets the apex, which must NOT carry noindex
- 1 cases: the live run targets the apex; the subdomain is a DNS-level concern
